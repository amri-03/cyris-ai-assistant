# 🧠 Cyris AI Assistant

A calm, continuity-aware personal AI assistant designed to maintain long-term context, adapt to your behavioral patterns, and support your long-term direction without the cognitive overload of traditional rigid productivity systems.

Unlike traditional checklist apps or cheerleading motivational assistants, Cyris adapts to you. It remembers your goals, struggles, academic timelines, and career milestones across sessions, helping you naturally resume progress without having to re-explain your context every time you chat.

---

## ✨ Key Capabilities

* **🧠 Persistent Continuity Memory**: A local JSON memory profile that saves key context details to maintain continuity across sessions.
* **⚡ Token-Efficient Extraction**: A hybrid pipeline with a local pre-filter that skips heavy LLM processing for general queries and only runs extraction on continuity updates.
* **⏳ Chronology & Conflict Resolution**: Tracks memory timestamps and automatically resolves conflicts by retiring outdated context when new updates are received.
* **👋 Dynamic Session Resumptions**: Greets you on startup with a warm, custom message generated by the AI referencing your active memory context.
* **🎛️ Interactive Memory Panel**: A sleek, glassmorphic side-drawer interface that allows you to inspect what Cyris currently remembers about you, delete items manually, or trigger memory reconciliations.

---

## 🛠️ Technical Stack

* **Frontend**: React, Vite, styled with custom Vanilla CSS variables and TailwindCSS.
* **Backend**: FastAPI (Python), Python Dotenv.
* **AI Providers**: Gemini API (`gemini-2.0-flash`), Groq API (`llama-3.3-70b-versatile`, `llama-3.1-8b-instant`).
* **Persistence**: Local JSON-based serialization.

---

## 📂 Codebase Overview

```text
├── backend/                   # FastAPI Python server
│   ├── app/
│   │   ├── main.py            # API endpoints (/chat, /memory-status, etc.)
│   │   ├── memory/            # Continuity & history services
│   │   ├── services/ai/       # AI provider clients (Gemini, Groq)
│   │   └── models/            # Datastructures & schema models
│   └── data/                  # Local persistence files (user_continuity.json)
│
├── frontend/                  # React + Vite client app
│   ├── src/
│   │   ├── components/        # Header, MessageBubble, MemoryPanel
│   │   ├── pages/Home.jsx     # Main conversational workspace
│   │   └── styles/global.css  # Ambient dark theme CSS variables
│
└── docs/                      # Architectural docs & development logs
```

---

## 🚀 Getting Started

To install and run Cyris on your local machine, you can expand the step-by-step setup guide below, or refer to the standalone **[SETUP.md](./SETUP.md)** file:

<details>
<summary><b>🛠️ Click to expand the Setup & Installation Guide</b></summary>

### Prerequisites

Before starting, ensure you have the following installed on your system:
* **Python 3.10+** (Ensure Python is added to your system PATH)
* **Node.js (v16+) and npm**

---

### 1. Clone the Repository

Open your terminal and clone the repository, then navigate to the root directory:

```bash
git clone https://github.com/amri-03/cyris-ai-assistant.git
cd cyris-ai-assistant
```

---

### 2. Backend Setup (FastAPI)

Navigate to the `backend` directory:

```bash
cd backend
```

#### Create a Python Virtual Environment

* **Windows (PowerShell/CMD)**:
  ```bash
  python -m venv venv
  ```
* **macOS / Linux**:
  ```bash
  python3 -m venv venv
  ```

#### Activate the Virtual Environment

* **Windows (PowerShell)**:
  ```powershell
  .\venv\Scripts\Activate.ps1
  ```
* **Windows (CMD)**:
  ```cmd
  .\venv\Scripts\activate.bat
  ```
* **macOS / Linux**:
  ```bash
  source venv/bin/activate
  ```

#### Install Python Dependencies

Make sure your virtual environment is active (you should see `(venv)` in your terminal prompt), then run:

```bash
pip install -r requirements.txt
```

---

### 3. Configure Environment Variables

Create a file named `.env` in the `backend/` folder:

```bash
# On Windows (CMD/PowerShell) or macOS/Linux:
# Copy the example file to get started
cp .env.example .env
```

Open the `.env` file and configure your API keys and provider selection:

```env
# Choose your main chat provider: "gemini" or "groq"
AI_PROVIDER=gemini

# If using Gemini (highly recommended for both Chat and Memory extraction)
GEMINI_API_KEY=your_gemini_api_key_here

# If using Groq (Llama models)
GROQ_API_KEY=your_groq_api_key_here
```

---

### 4. Run the Backend Server

Start the FastAPI application by executing `run.py` in the `backend/` folder:

```bash
python run.py
```

The backend API server will start up with auto-reload enabled at:
👉 **`http://127.0.0.1:8000`**

---

### 5. Frontend Setup (React + Vite)

Open a **new terminal window**, navigate to the `frontend` folder, install the Node packages, and run the development server:

```bash
cd frontend
npm install
npm run dev
```

The frontend client will start running at:
👉 **`http://localhost:5173`**

---

### 6. Access Cyris AI Assistant

Open your web browser and navigate to **`http://localhost:5173`** to begin chatting with Cyris!
* Click the **Memory Profile (Brain)** icon button in the top right header to toggle open the memory dashboard modal.
* Chat naturally, and Cyris will extract goals/struggles and automatically update your profile.

</details>

---

<details>
<summary><b>⏱️ Development Progress Timeline (Collapsible)</b></summary>

* ✅ Phase 0 — Project Initialization & Foundation Setup
* ✅ Phase 1 — Foundational Architecture & Backend Environment
* ✅ Phase 2 — Core Intelligence Architecture Planning
* ✅ Phase 3 — Backend Intelligence Structure
* ✅ Phase 4 — Memory & Context Intelligence Foundation
* ✅ Phase 5 — Persistent Intelligence & User Lifecycle Architecture
* ✅ Phase 6 — Adaptive Behavioral Intelligence Architecture
* ✅ Phase 7 — Operational Intelligence & Execution Flow Architecture
* ✅ Phase 8 — Persistent Runtime Intelligence Architecture
* ✅ Phase 9 — Conversational Intelligence Foundation
* ✅ Phase 10 — Behavioral Understanding & Memory Intelligence
* ✅ Phase 11 — AI Integration & Contextual Intelligence Coordination
* ✅ Phase 12 — Adaptive Frontend Interaction Architecture
* ✅ Phase 13 — Adaptive Intelligence Refinement & System Cohesion
* ✅ Phase 14 — Deployment Readiness & Real-World Stabilization
* ✅ Phase 15 — Real-World MVP Refinement & Validation
* ✅ Phase 16 — Conversational Continuity & Frontend Transformation (Finalized)
</details>