export default function InteractionStatusCard({
                                                  status
                                              }) {

    return (
        <div>

            <h3>
                Interaction Status
            </h3>

            <p>
                {
                    status
                }
            </p>

        </div>
    );
}