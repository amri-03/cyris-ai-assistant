export default function SystemHealthOverview({
                                                 systemHealth
                                             }) {

    return (

        <div>

            <h3>
                System Health
            </h3>

            <p>
                {systemHealth}
            </p>

        </div>
    );
}